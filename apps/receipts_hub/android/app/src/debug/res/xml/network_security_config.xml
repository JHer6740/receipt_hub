<?xml version="1.0" encoding="utf-8"?>
<!--
  Debug only. Never shipped.

  Allows plain HTTP to the loopback addresses a local backend runs on, so the
  developer host screen can reach it. Everything else still requires HTTPS, so
  a debug build cannot accidentally talk to a real service in the clear.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
    <domain-config cleartextTrafficPermitted="true">
        <!-- The emulator's alias for the host machine. -->
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
